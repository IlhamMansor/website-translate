<%- include('partials/head') %>
<body>
  <%- include('partials/nav') %>

    <div class="container">
        <h1>Result for <%= foundWord.word %> :</h1>
        <div class="row">
        <div class="col-md-6 col-lg-4 d-flex justify-content-center">
            <div class="card" style="width: 18rem;">
                <% if(foundWord.img) { %>
                    <img src="<%= foundWord.img %> " class="card-img-top" alt="<%= foundWord.word %>">
                <% } %>
                    <div class="card-body">
                        <h5 class="card-title"><%= foundWord.word %></h5>
                        <p class="card-text"><%= foundWord.description %></p>
                    </div>
                    <div class="row">
                      <div class="col d-flex justify-content-center">
                      <form action="/<%= foundWord.word %>/edit" method="get">
                        <button type="submit" class="btn btn-dark">Edit</button>
                      </form>
                      </div>

                      <div class="col d-flex justify-content-center">
                        <form action="/<%= foundWord.word %>?_method=DELETE" method="post">
                          <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                        </div>
                    </div>
            </div>
        </div>
        </div>
    </div>
</body>
</html>